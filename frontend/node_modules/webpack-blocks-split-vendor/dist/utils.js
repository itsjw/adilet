'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
const hasBeenExcluded = exports.hasBeenExcluded = (userRequest, exclude) => {
  if (!exclude) {
    return false;
  } else if (Array.isArray(exclude)) {
    return exclude.some(item => hasBeenExcluded(userRequest, item));
  } else if (exclude instanceof RegExp) {
    return exclude.test(userRequest);
  }

  return userRequest.indexOf(exclude) >= 0;
};
const isVendor = exports.isVendor = exclude => ({ userRequest }) => userRequest && userRequest.indexOf('node_modules') >= 0 && userRequest.match(/\.js$/) && !hasBeenExcluded(userRequest, exclude);